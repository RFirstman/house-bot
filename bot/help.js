module.exports = () => {
	return `Available commands:
	  menu
	    - get this week's menu 
	    - add 'today' for today's menu
	    - or 'tomorrow' for tomorrow's
	  weather
	    - get the current weather
	  whosecar <brand/make>
		- returns who the <brand/make> belongs to`;
};
